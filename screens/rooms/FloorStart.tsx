import {SafeAreaView} from "react-native";
import {Layout} from "@ui-kitten/components";

export const FloorStart = () => {
    return {
        <SafeAreaView>
            <Layout>
                You descent the stairs and the first floor seems to be full of plants etc blablabla. You see X doors. Which path do you want to take?
            </Layout>
        </SafeAreaView>
    }
}